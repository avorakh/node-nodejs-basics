import { join } from 'path';
import { readdir } from 'fs/promises';
import { currentDirectoryPath, isExistingPath } from './utils.js'

const read = async () => {fileToRead.txt
    // Write your code here 
};

await read();